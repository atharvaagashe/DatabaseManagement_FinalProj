<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Reviews</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

    <div class="container mt-5">
        <h2 class="mb-4">Reviews for Movie ID: {{movieId}}</h2>

        <a href="/movies" class="btn btn-secondary mb-4">← Back to Movies</a>

        {{#reviews}}
        <div class="card mb-3">
            <div class="card-body">
                <h5 class="card-title">Rating: {{rating}} / 10</h5>
                <p class="card-text">{{comment}}</p>
                <small class="text-muted">Posted by user {{userId}} on {{timestamp}}</small>
            </div>
        </div>
        {{/reviews}}

        <h4 class="mt-5">Add Your Review</h4>
        <form method="post" action="/reviews">
            <input type="hidden" name="movieId" value="{{movieId}}">
            <input type="hidden" name="userId" value="1"> <!-- Replace with session value if available -->
            <div class="mb-3">
                <label for="rating" class="form-label">Rating (1–10):</label>
                <input type="number" class="form-control" id="rating" name="rating" min="1" max="10" required>
            </div>
            <div class="mb-3">
                <label for="comment" class="form-label">Comment:</label>
                <textarea class="form-control" id="comment" name="comment" rows="3" required></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Submit Review</button>
        </form>
    </div>

</body>
</html>
