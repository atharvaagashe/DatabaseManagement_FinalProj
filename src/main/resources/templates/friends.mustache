<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Friends - Popcorn Opinions</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            background-color: #121212;
            color: #f1f1f1;
            font-family: 'Inter', sans-serif;
        }

        .navbar {
            background-color: #1f1f1f;
        }

        .card {
            background-color: #1e1e1e;
            border: none;
            transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
            color: #f1f1f1;
            margin-bottom: 15px;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.08);
        }

        .btn-primary {
            background-color: #0066ff;
            border: none;
        }

        .btn-primary:hover {
            background-color: #0052cc;
        }
        
        .btn-danger {
            background-color: #dc3545;
            border: none;
        }
        
        .btn-danger:hover {
            background-color: #bb2d3b;
        }

        .text-muted {
            color: #aaaaaa !important;
        }

        h2.fw-bold {
            color: #ffffff;
        }

        a.nav-link {
            color: #e0e0e0 !important;
        }

        a.nav-link:hover {
            text-decoration: underline;
        }

        .user-card {
            padding: 20px;
        }

        .user-email {
            color: #aaaaaa;
            font-size: 0.9rem;
        }

        .user-username {
            font-weight: 600;
            font-size: 1.1rem;
        }

        .section-title {
            margin-bottom: 25px;
            border-bottom: 1px solid #333;
            padding-bottom: 10px;
        }
        
        .alert {
            margin-bottom: 20px;
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark mb-4">
    <div class="container-fluid">
        <a class="navbar-brand" href="/dashboard">Popcorn Opinions</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item"><a class="nav-link" href="/movies">Movies</a></li>
                <li class="nav-item"><a class="nav-link" href="/user-reviews">Reviews/Ratings</a></li>
                <li class="nav-item"><a class="nav-link" href="/watchlist">Watchlist</a></li>
                <li class="nav-item"><a class="nav-link" href="/user">User Info</a></li>
                <li class="nav-item"><a class="nav-link active" href="/users-list">Friends</a></li>
            </ul>
        </div>
    </div>
</nav>

<div class="container">
    <h2 class="fw-bold text-center mb-4">Users on Popcorn Opinions</h2>
    <p class="text-muted text-center mb-5">Connect with other movie enthusiasts on the platform.</p>
    
    {{#successMessage}}
    <div class="alert alert-success">{{successMessage}}</div>
    {{/successMessage}}
    
    {{#errorMessage}}
    <div class="alert alert-danger">{{errorMessage}}</div>
    {{/errorMessage}}

    <div class="row">
        <div class="col-lg-12">
            <h4 class="section-title">All Users</h4>
            
            {{#allUsers}}
            <div class="card user-card" data-user-id="{{userId}}">
                <div class="row align-items-center">
                    <div class="col-md-8">
                        <div class="user-username">{{username}}</div>
                        <div class="user-email">{{email}}</div>
                    </div>
                    <div class="col-md-4 text-end">
                        {{#isCurrentUser}}
                            <span class="badge bg-secondary">You</span>
                        {{/isCurrentUser}}
                        {{^isCurrentUser}}
                            {{#isFriend}}
                                <a href="/remove-friend/{{userId}}" class="btn btn-danger">Remove Friend</a>
                            {{/isFriend}}
                            {{^isFriend}}
                                <a href="/add-friend/{{userId}}" class="btn btn-primary">Add Friend</a>
                            {{/isFriend}}
                        {{/isCurrentUser}}
                    </div>
                </div>
            </div>
            {{/allUsers}}
            
            {{^allUsers}}
            <div class="alert alert-info">No other users found.</div>
            {{/allUsers}}
        </div>
    </div>
</div>

</body>
</html>
