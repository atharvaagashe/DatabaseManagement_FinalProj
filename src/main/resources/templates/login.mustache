<!DOCTYPE html>
<html>
<head>
    <title>Login - CineScope</title>
    <style>
        body {
            background-color: #121212;
            font-family: 'Segoe UI', sans-serif;
            color: #fff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .login-container {
            background-color: #1e1e1e;
            padding: 2rem;
            border-radius: 12px;
            width: 320px;
            box-shadow: 0 0 20px rgba(0,0,0,0.5);
        }

        h2 {
            text-align: center;
            margin-bottom: 1rem;
        }

        input {
            width: 100%;
            padding: 10px;
            margin: 0.5rem 0;
            border: none;
            border-radius: 6px;
        }

        .login-button {
            background-color: #03A9F4;
            color: white;
            font-weight: bold;
            cursor: pointer;
        }

        .login-button:hover {
            background-color: #0288D1;
        }

        a {
            display: block;
            text-align: center;
            margin-top: 1rem;
            color: #bbb;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <!-- Logo added here -->
        <div style="text-align: center; margin-bottom: 1rem;">
            <img src="/images/logo.jpg" alt="CineScope Logo" style="width: 100px; height: 100px; object-fit: cover; border-radius: 50%; box-shadow: 0 0 8px rgba(255,255,255,0.1);">
        </div>

        <h2>Login</h2>
        <form method="post" action="/login">
            <input type="text" name="username" placeholder="Username" required>
            <input type="password" name="password" placeholder="Password" required>
            <input type="submit" class="login-button" value="Log In">
        </form>
        <a href="/signup">Don't have an account? Sign up</a>
    </div>
</body>
</html>