<!DOCTYPE html>
<html>
<head>
    <title>Welcome to CineScope</title>
    <style>
        body {
            background-color: #121212;
            color: #fff;
            font-family: 'Segoe UI', sans-serif;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .home-container {
            text-align: center;
            background-color: #1e1e1e;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 0 20px rgba(0,0,0,0.5);
            width: 400px;
        }

        img {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 1rem;
            box-shadow: 0 0 8px rgba(255,255,255,0.1);
        }

        h1 {
            margin-bottom: 1rem;
        }

        a {
            display: inline-block;
            padding: 10px 20px;
            margin: 0.5rem;
            text-decoration: none;
            border-radius: 6px;
            font-weight: bold;
        }

        .signup-button {
            background-color: #4CAF50;
            color: white;
        }

        .login-button {
            background-color: #03A9F4;
            color: white;
        }

        .signup-button:hover {
            background-color: #43A047;
        }

        .login-button:hover {
            background-color: #0288D1;
        }
    </style>
</head>
<body>
    <div class="home-container">
        <img src="/images/logo.jpg" alt="CineScope Logo">
        <h1>Welcome to CineScope</h1>
        <a href="/signup" class="signup-button">Sign Up</a>
        <a href="/login" class="login-button">Login</a>
    </div>
</body>
</html>